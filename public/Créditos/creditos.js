// Função de placeholder para o botão "Ver Time"
function showTeam(memberName) {
    alert(`${memberName}: Desconhecida [DEBUGGING]`);
}

// Função de placeholder para o botão "Ver Detalhes"
function showDetails(memberName) {
    alert(`[DEBUG] Visualizando detalhes de: ${memberName}\n(Função ainda não implementada)`);
}